# ==========================================
# CORE SETTINGS
# ==========================================
set-option -g status-position top
set-option -g default-shell /bin/zsh
set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm-256color:RGB"
set -g mouse on
set-option -g allow-rename on
set-window-option -g automatic-rename on
set -g renumber-windows on

bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

bind -n M-Left resize-pane -L 5
bind -n M-Right resize-pane -R 5
bind -n M-Up resize-pane -U 5
bind -n M-Down resize-pane -D 5

unbind ','
bind ',' display-popup -E -w 50% -h 15% -S "fg=#7aa2f7,bg=#16161e" -b rounded -s "fg=#7aa2f7,bg=#16161e" "bash -c \"echo -e '\\033[38;2;21;22;30m\\033[48;2;122;162;247m Enter Tmux Command: \\033[0m'; read -p '> ' cmd && tmux \\\$cmd\""

unbind '&'
bind '&' display-popup -E -w 40% -h 15% -S "fg=#7aa2f7,bg=#16161e" -b rounded -s "fg=#7aa2f7,bg=#16161e" "bash -c \"echo -e '\\033[38;2;21;22;30m\\033[48;2;122;162;247m Kill current window? (y/n): \\033[0m'; read -n 1 -r reply; if [[ \\\$reply =~ ^[Yy]$ ]]; then tmux kill-window; fi\""

unbind 'x'
bind 'x' display-popup -E -w 40% -h 15% -S "fg=#7aa2f7,bg=#16161e" -b rounded -s "fg=#7aa2f7,bg=#16161e" "bash -c \"echo -e '\\033[38;2;21;22;30m\\033[48;2;122;162;247m Kill current pane? (y/n): \\033[0m'; read -n 1 -r reply; if [[ \\\$reply =~ ^[Yy]$ ]]; then tmux kill-pane; fi\""

# ==========================================
# VIM-MODE & COPY-PASTE (macOS pbcopy)
# ==========================================
setw -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"

# ==========================================
# CUSTOM THEME (BLUE ACCENT)
# ==========================================
set -g mode-style "fg=#7aa2f7,bg=#3b4261"
set -g message-style "fg=#7aa2f7,bg=#3b4261"
set -g message-command-style "fg=#7aa2f7,bg=#3b4261"
set -g pane-border-style "fg=#3b4261"
set -g pane-active-border-style "fg=#7aa2f7"

set -g status "on"
set -g status-justify "left"
set -g status-style "fg=#7aa2f7,bg=#16161e"
set -g status-left-length "100"
set -g status-right-length "100"
set -g status-left-style NONE
set -g status-right-style NONE

set -g status-left "#[fg=#15161e,bg=#7aa2f7,bold] #S #[fg=#7aa2f7,bg=#16161e,nobold,nounderscore,noitalics]"

set -g status-right "#[fg=#3b4261,bg=#16161e,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=#3b4261] %Y-%m-%d  %I:%M %p #[fg=#7aa2f7,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#15161e,bg=#7aa2f7,bold] #h ❯"

setw -g window-status-format "#[fg=#16161e,bg=#16161e,nobold,nounderscore,noitalics]#[default] #I  #W #F #[fg=#16161e,bg=#16161e,nobold,nounderscore,noitalics]"

setw -g window-status-current-format "#[fg=#16161e,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#7aa2f7,bg=#3b4261,bold] #I  #W #F #[fg=#3b4261,bg=#16161e,nobold,nounderscore,noitalics]"

# ==========================================
# FUNCTIONALITY PLUGINS (TPM)
# ==========================================
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

set -g @continuum-restore 'on'
set -g @resurrect-capture-pane-contents 'on'

# ==========================================
# TPM INITIALIZATION
# ==========================================
run -b "$HOME/.tmux/plugins/tpm/tpm"
